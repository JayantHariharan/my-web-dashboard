[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "playnexus"
version = "0.1.0"
description = "PlayNexus backend"
requires-python = ">=3.11,<3.13"

[tool.setuptools]
packages = ["backend"]
